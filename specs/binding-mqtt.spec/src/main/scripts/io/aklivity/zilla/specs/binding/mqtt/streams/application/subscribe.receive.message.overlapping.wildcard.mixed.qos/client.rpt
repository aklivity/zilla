#
# Copyright 2021-2023 Aklivity Inc.
#
# Aklivity licenses this file to you under the Apache License,
# version 2.0 (the "License"); you may not use this file except in compliance
# with the License. You may obtain a copy of the License at:
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.
#

connect "zilla://streams/app0"
         option zilla:window 8192
         option zilla:transmission "duplex"

write zilla:begin.ext ${mqtt:beginEx()
                            .typeId(zilla:id("mqtt"))
                            .session()
                              .flags("CLEAN_START")
                              .clientId("client")
                              .build()
                            .build()}

read zilla:begin.ext ${mqtt:matchBeginEx()
                              .typeId(zilla:id("mqtt"))
                              .session()
                                .flags("CLEAN_START")
                                .qosMax(2)
                                .packetSizeMax(66560)
                                .capabilities("RETAIN", "WILDCARD", "SUBSCRIPTION_IDS", "SHARED_SUBSCRIPTIONS")
                                .clientId("client")
                                .build()
                              .build()}

connected

read zilla:data.empty

write zilla:data.ext ${mqtt:dataEx()
                           .typeId(zilla:id("mqtt"))
                           .session()
                              .kind("STATE")
                              .build()
                           .build()}

write ${mqtt:session()
            .subscription("sensor/+/#", 1, "AT_LEAST_ONCE", "SEND_RETAINED")
            .build()}
write flush

read ${mqtt:session()
           .subscription("sensor/+/#", 1, "AT_LEAST_ONCE", "SEND_RETAINED")
           .build()}
read notify RECEIVED_SESSION_STATE

write zilla:data.ext ${mqtt:dataEx()
                           .typeId(zilla:id("mqtt"))
                           .session()
                              .kind("STATE")
                              .build()
                           .build()}

write ${mqtt:session()
            .subscription("sensor/+/#", 1, "AT_LEAST_ONCE", "SEND_RETAINED")
            .subscription("sensor/+/1", 2, "EXACTLY_ONCE", "SEND_RETAINED")
            .build()}

read ${mqtt:session()
           .subscription("sensor/+/#", 1, "AT_LEAST_ONCE", "SEND_RETAINED")
           .subscription("sensor/+/1", 2, "EXACTLY_ONCE", "SEND_RETAINED")
           .build()}


connect await RECEIVED_SESSION_STATE
        "zilla://streams/app0"
         option zilla:window 8192
         option zilla:transmission "duplex"

write zilla:begin.ext ${mqtt:beginEx()
                             .typeId(zilla:id("mqtt"))
                             .subscribe()
                                .clientId("client")
                                .filter("sensor/+/#", 1, "AT_MOST_ONCE", "SEND_RETAINED")
                                .build()
                             .build()}

connected

write advise zilla:flush ${mqtt:flushEx()
                                 .typeId(zilla:id("mqtt"))
                                 .subscribe()
                                     .filter("sensor/+/#", 1, "AT_MOST_ONCE", "SEND_RETAINED")
                                     .filter("sensor/+/1", 2, "AT_MOST_ONCE", "SEND_RETAINED")
                                     .build()
                                 .build()}


connect await RECEIVED_SESSION_STATE
        "zilla://streams/app0"
         option zilla:window 8192
         option zilla:transmission "duplex"

write zilla:begin.ext ${mqtt:beginEx()
                             .typeId(zilla:id("mqtt"))
                             .subscribe()
                                .clientId("client")
                                .filter("sensor/+/#", 1, "AT_LEAST_ONCE", "SEND_RETAINED")
                                .build()
                             .build()}

connected

write advise zilla:flush ${mqtt:flushEx()
                                 .typeId(zilla:id("mqtt"))
                                 .subscribe()
                                     .filter("sensor/+/#", 1, "AT_LEAST_ONCE", "SEND_RETAINED")
                                     .filter("sensor/+/1", 2, "AT_LEAST_ONCE", "SEND_RETAINED")
                                     .build()
                                 .build()}

read zilla:data.ext ${mqtt:matchDataEx()
                             .typeId(zilla:id("mqtt"))
                             .subscribe()
                                 .topic("sensor/one/1")
                                 .packetId(1)
                                 .qos("EXACTLY_ONCE")
                                 .subscriptionId(1)
                                 .subscriptionId(2)
                                 .format("TEXT")
                                 .build()
                             .build()}
read "message"


connect await RECEIVED_SESSION_STATE
        "zilla://streams/app0"
         option zilla:window 8192
         option zilla:transmission "duplex"

write zilla:begin.ext ${mqtt:beginEx()
                             .typeId(zilla:id("mqtt"))
                             .subscribe()
                                .clientId("client")
                                .filter("sensor/+/1", 2, "EXACTLY_ONCE", "SEND_RETAINED")
                                .build()
                             .build()}

connected

read zilla:data.ext ${mqtt:matchDataEx()
                             .typeId(zilla:id("mqtt"))
                             .subscribe()
                                 .topic("sensor/one/1")
                                 .packetId(1)
                                 .qos("EXACTLY_ONCE")
                                 .subscriptionId(1)
                                 .subscriptionId(2)
                                 .format("TEXT")
                                 .build()
                             .build()}
read "message"

write advise zilla:flush ${mqtt:flushEx()
                                 .typeId(zilla:id("mqtt"))
                                 .subscribe()
                                     .packetId(1)
                                     .build()
                                 .build()}
