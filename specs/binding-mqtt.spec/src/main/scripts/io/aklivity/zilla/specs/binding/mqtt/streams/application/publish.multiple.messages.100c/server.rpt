#
# Copyright 2021-2026 Aklivity Inc.
#
# Aklivity licenses this file to you under the Apache License,
# version 2.0 (the "License"); you may not use this file except in compliance
# with the License. You may obtain a copy of the License at:
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.
#

accept "zilla://streams/app0"
        option zilla:window 8192
        option zilla:padding 100
        option zilla:transmission "duplex"

accepted

read zilla:begin.ext ${mqtt:matchBeginEx()
                             .typeId(zilla:id("mqtt"))
                             .session()
                                .flags("CLEAN_START")
                                .clientId("client")
                                .build()
                             .build()}

write zilla:begin.ext ${mqtt:beginEx()
                              .typeId(zilla:id("mqtt"))
                              .session()
                                .flags("CLEAN_START")
                                .subscribeQosMax(2)
                                .publishQosMax(2)
                                .packetSizeMax(33792)
                                .capabilities("RETAIN", "WILDCARD", "SUBSCRIPTION_IDS", "SHARED_SUBSCRIPTIONS")
                                .clientId("client")
                                .build()
                              .build()}

connected

write zilla:data.empty
write flush


accepted

read zilla:begin.ext ${mqtt:matchBeginEx()
                             .typeId(zilla:id("mqtt"))
                             .publish()
                                .clientId("client")
                                .topic("sensor/one")
                                .build()
                             .build()}

connected

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00001-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00002-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00003-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00004-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00005-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00006-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00007-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00008-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00009-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00010-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00011-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00012-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00013-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00014-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00015-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00016-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00017-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00018-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00019-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00020-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00021-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00022-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00023-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00024-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00025-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00026-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00027-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00028-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00029-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00030-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00031-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00032-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00033-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00034-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00035-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00036-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00037-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00038-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00039-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00040-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00041-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00042-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00043-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00044-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00045-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00046-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00047-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00048-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00049-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00050-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00051-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00052-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00053-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00054-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00055-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00056-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00057-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00058-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00059-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00060-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00061-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00062-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00063-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00064-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00065-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00066-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00067-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00068-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00069-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00070-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00071-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00072-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00073-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00074-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00075-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00076-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00077-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00078-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00079-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00080-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00081-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00082-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00083-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00084-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00085-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00086-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00087-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00088-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00089-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00090-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00091-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00092-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00093-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00094-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00095-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00096-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00097-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00098-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00099-test-payloadxxxxxxxx"

read zilla:data.ext ${mqtt:matchDataEx()
                          .typeId(zilla:id("mqtt"))
                          .publish()
                             .build()
                          .build()}

read "seq-00100-test-payloadxxxxxxxx"

