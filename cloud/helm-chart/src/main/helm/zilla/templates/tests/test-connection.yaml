apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zilla.fullname" . }}-test-connection"
  labels:
    {{- include "zilla.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zilla.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
