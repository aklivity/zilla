version: "3"

networks:
  net0:
    driver: overlay

services:

  zilla:
    image: "ghcr.io/aklivity/zilla:develop-SNAPSHOT"
    hostname: "zilla"
    command: [ "start", "-v", "-e" ]
    volumes:
      - ./conf:/etc/zilla:ro
    networks:
      - net0
    environment:
      - KEYSTORE_PASSWORD=generated
    ports:
      - "5671:5671"
      - "5672:5672"
