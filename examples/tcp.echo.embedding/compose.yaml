name: ${NAMESPACE:-zilla-tcp-echo-embedding}
services:
  zilla:
    image: ghcr.io/aklivity/zilla:${ZILLA_VERSION:-latest}
    restart: unless-stopped
    hostname: zilla.examples.dev
    ports:
      - 12345:12345
    healthcheck:
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 15s
      test: ["CMD", "sh", "-c", "test -f /var/run/zilla/ready"]
    environment:
      ZILLA_INCUBATOR_ENABLED: "true"
    volumes:
      - ./etc:/etc/zilla
      - glove-cache:/var/run/zilla/cache
    command: start -v -e

volumes:
  glove-cache:

networks:
  default:
    driver: bridge
