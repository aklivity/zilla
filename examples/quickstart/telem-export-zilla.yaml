name: telemetry-exporters
telemetry:
  exporters:
    # Prometheus endpoint definition
    prometheus_metric_exporter:
      type: prometheus
      options:
        endpoints:
          - scheme: http
            path: /metrics
            port: 7190
